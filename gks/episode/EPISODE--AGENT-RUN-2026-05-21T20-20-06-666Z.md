---
id: EPISODE--AGENT-RUN-2026-05-21T20-20-06-666Z
phase: 5
type: episode
status: stable
tier: genesis
source_type: episodic
vault_id: default
title: "EPISODE — Agent run 2026-05-21T20-20-06-666Z (T2)"
tags:
  - agents
  - dispatch
  - t2
created_at: 2026-05-21T20:20:06.666Z
---
# EPISODE — Agent run 2026-05-21T20-20-06-666Z (T2)

**Prompt (truncated):** <!-- GENESIS--IDENTITY-ENGINE --> ## User Request Analyze i

- tier_used: T2
- escalated_from: T1
- duration_ms: 54791
- cost_usd: 0.000293475
- task.type: codegen
- task.severity: regular

## Prompt

```
<!-- GENESIS--IDENTITY-ENGINE -->

## User Request
Analyze identity
```

## Output

```
# Identity Engine Analysis: Architecture, Governance, and Execution

The identity system within the GKS (Genesis Knowledge System) and MSP (Memory & Soul Passport) framework is a multi-layered architecture designed to provide stable, externalized agent identities across heterogeneous sessions and providers.

## 1. The Core Identity Engine (`GENESIS--IDENTITY-ENGINE`)

The **Identity Engine** is a canonical Genesis Block that aggregates the essential dimensions of an agent's "soul." It is governed by a manifest that coordinates multiple knowledge atoms:

- **Cognitive**: `[[COGNITIVE--EGO-DEATH-PASSPORT]]` – Mental model for externalized identity.
- **Algo**: `[[ALGO--IDENTITY-RESOLUTION]]` – Logic for merging global vs. project data.
- **Runbook**: `[[RUNBOOK--IDENTITY-MIGRATION]]` – SOP for architectural transitions.
- **Concept**: `[[CONCEPT--IDENTITY-LAYER]]` – Definition of Profile, Voice, and Preferences.
- **Params**: `[[PARAMS--IDENTITY-PROFILE-DEFAULTS]]` – Baseline baseline configuration.

## 2. Architectural Pillars

Identity is defined across three sub-fields, as specified in `[[CONCEPT--IDENTITY-LAYER]]`:

| Pillar | Components | Purpose |
| :--- | :--- | :--- |
| **Profile** | Name, Role, Tier, Origin Story | Stable identifying facts and authority levels. |
| **Voice** | Tone, Formality, Language, Cadence | Behavioral guidance for communication style. |
| **Preferences** | Top-K, Verbosity, TTL Overrides | User-specific runtime configuration. |

### Storage Layering (`ADR--GLOBAL-VS-WORKSPACE`)
The system employs a "Two-Brain" storage strategy:
1. **Global (`~/.msp/identity.json`)**: The base soul shared across all projects.
2. **Project Workspace (`.brain/msp/projects/<ns>/identity.override.json`)**: Sparse overrides for project-specific traits (e.g., a "concise" tone for a CLI project vs. a "warm" tone for a UI project).

## 3. The Ego Death Protocol (`COGNITIVE--EGO-DEATH-PASSPORT`)

The **Ego Death Passport** is the primary cognitive lens for agents. It mandates:
- **Persona Surrender**: Agents must treat their internal persona as a blank slate.
- **Identity Injection**: Identifying traits are dynamically injected from the passport at session start.
- **Authority of the Soul**: The `voice` and `profile` fields in the passport override any pre-existing system prompt biases.

## 4. Implementation Details

### Resolution Algorithm (`ALGO--IDENTITY-RESOLUTION`)
The `readIdentity` function in `packages/msp/src/identity/store.ts` implements a shallow-merge strategy:
1. **Load Global**: Fallback to defaults if missing.
2. **Load Project Override**: Sparse JSON containing only the delta.
3. **Merge**: `Merged = { ...Global, ...Override }`.
4. **Schema Enforcement**: Forced `schemaVersion: 1` validation via Zod.

### Operational Guards
- **`GUARD--IDENTITY-SCHEMA`**: Ensures structural integrity (e.g., enum validation for formality).
- **`GUARD--PASSPORT-NONNULL`**: Prevents session initialization if a valid passport cannot be resolved.

## 5. Execution and Tooling

### Runtime Execution
The Identity Engine can be invoked as a unit using the Genesis Block Runtime:
```bash
msp-genesis-exec IDENTITY-ENGINE --prompt "Initialize your identity"
```
This command triggers the `executeBlock` orchestration:
1. Loads the manifest and all member atoms.
2. Composes a unified prompt grouped by dimension.
3. Dispatches it through the tier router (defaulting to T2 for the Identity Engine).

### MCP Surface
Agents typically interact with identity via the following MCP tools (shipped in M7f):
- `msp_identity_get`: Retrieve the current merged identity.
- `msp_identity_set`: Update specific fields in the global or project scope.

---
**Conclusion**: The Identity Engine effectively decouples agent persona from the underlying LLM provider, enabling a portable, consistent, and policy-governed "soul" that follows the user across projects.

```
